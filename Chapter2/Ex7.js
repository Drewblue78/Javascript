// Define three functions—negate, intensify, and reinforce—that take a string and add the 
// prefixes “un”, “plus”, and “double” to that string, respectively.Your function definitions 
// should allow you to generate the following console session:

function negate(str) {
  return "un" + str;
}

function intensify(str) {
  return "plus" + str;
}

function reinforce(str) {
  return "doublepluscold" + str
}
