// Write a function sumFirstNIntegers that takes the value of N as its argument and
// returns the sum of those integers, as illustrated in the following sample run:

// N×(N+1)/2

function sumFirstNIntegers(n) {
  (n * (n + 1)) / 2
}